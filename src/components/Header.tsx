import React from "react";

const Header: React.FC = () => {
  return (
    <header className="w-full p-4 border-b border-b-black italic font-bold">
      Wanted Pre-onboarding course
    </header>
  );
};

export default Header;
